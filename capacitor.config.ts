import type { CapacitorConfig } from '@capacitor/cli';

const config: CapacitorConfig = {
  appId: 'com.radar.mercadopublico',
  appName: 'Radar Mercado Publico',
  webDir: 'dist'
};

export default config;
